type IconType = {
  class?: string;
  color?: string;
};

type Watcher = {
  id: string;
  name: string;
  image: string;
  addProfile?: boolean;
};

type ThemeType = {
  primaryRed: string;
  bg: string;
};
